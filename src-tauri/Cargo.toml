[package]
name = "md-viewer-tauri"
version = "0.1.1"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
tauri = { version = "2.0.0", features = ["protocol-asset"] }
rfd = "0.15"
